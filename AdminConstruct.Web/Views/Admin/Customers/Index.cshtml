@model IEnumerable<AdminConstruct.Web.ViewModels.CustomerViewModel>



@{
    ViewData["Title"] = "Clientes";
}

<h1>Gestión de Clientes</h1>

<form method="get" class="mb-3">
    <input type="text" name="search" placeholder="Buscar por nombre o documento" class="form-control" value="@Context.Request.Query["search"]" />
</form>

<div class="mb-3">
    <a asp-controller="Export" asp-action="ExportProductsToExcel" class="btn btn-primary">
        📥 Exportar Productos a Excel
    </a>
    <a asp-controller="Export" asp-action="ExportClientsToPDF" class="btn btn-secondary">
        📝 Exportar Clientes a PDF
    </a>
</div>


<table class="table table-striped">
    <thead>
    <tr>
        <th>Nombre</th>
        <th>Documento</th>
        <th>Email</th>
        <th>Teléfono</th>
        <th>Acciones</th>
    </tr>
    </thead>
    <tbody>
    @foreach (var c in Model)
    {
        <tr>
            <td>@c.Name</td>
            <td>@c.Document</td>
            <td>@c.Email</td>
            <td>@c.Phone</td>
            <td>
                <a asp-action="Details" asp-route-id="@c.Id" class="btn btn-info btn-sm">Detalles</a>
                <a asp-action="Delete" asp-route-id="@c.Id" class="btn btn-danger btn-sm">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>
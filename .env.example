# Ejemplo de variables de entorno para producción
# Copia este archivo a .env y configura tus valores

# Base de Datos
POSTGRES_DB=AdminConstructDb
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CAMBIA_ESTO_EN_PRODUCCION

# JWT (IMPORTANTE: Usa una clave segura de al menos 32 caracteres)
JWT_SECRET_KEY=CAMBIA_ESTO_POR_UNA_CLAVE_SUPER_SEGURA_DE_AL_MENOS_32_CARACTERES

# SMTP (Configuración de email)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tu-email@gmail.com
SMTP_PASSWORD=tu-app-password

# URLs de los servicios (cambiar en producción)
API_URL=http://localhost:5228
WEB_URL=http://localhost:5005
CLIENT_URL=http://localhost:3000

# ========================================
# AdminConstruct - Environment Configuration
# ========================================
# 
# INSTRUCTIONS:
# 1. Copy this file to .env
#    cp .env.example .env
# 2. Run: docker compose up --build
#
# These values are pre-configured to work with Docker out of the box.
# ========================================

# ========================================
# DATABASE CONFIGURATION
# ========================================
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=db
DB_HOST=db
DB_PORT=5432

# Connection String (uses Docker service name 'db')
CONNECTION_STRING=Host=db;Port=5432;Database=db;Username=user;Password=password

# ========================================
# JWT AUTHENTICATION CONFIGURATION
# ========================================
# Default key for development/testing
JWT_KEY=key
JWT_ISSUER=Admin
JWT_AUDIENCE=Admin

# ========================================
# SMTP EMAIL CONFIGURATION (Optional)
# ========================================
# Leave as is if not using email, or update with your credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password

# ========================================
# DEFAULT USER CREDENTIALS (Reference)
# ========================================
# Admin: admin@adminconstruct.com / Admin123!
# Client: client@example.com / Client123!
# ========================================
